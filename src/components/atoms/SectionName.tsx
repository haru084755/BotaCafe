export const SectionName = ({ name }: { name: string }) => {
  return (
    <h2 className="font-karla font-extralight tracking-wide text-5xl border-b-2 border-black inline-block pb-2">
      {name}
    </h2>
  )
}